{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "regions": ["sin1"],
  "crons": [
    { "path": "/api/cron", "schedule": "0 * * * 1-5" }
  ]
}